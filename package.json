{
  "name": "base-defi-bank",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w apps/web run build && npm -w packages/contracts run build",
    "dev": "npm -w apps/web run dev",
    "lint": "npm -w apps/web run lint",
    "format": "prettier --write .",
    "typecheck": "npm -w apps/web run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}